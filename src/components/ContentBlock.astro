---
import { normalizeMojibake } from "../utils/content.js";

interface Props {
  html?: string;
}

const { html } = Astro.props;
const normalizedHtml = normalizeMojibake(html || "");
---

{normalizedHtml && <section class="content-block" set:html={normalizedHtml} />}
