---
import { cleanHtml } from "../utils/content.js";

interface Props {
  html?: string;
}

const { html } = Astro.props;
const normalizedHtml = cleanHtml(html || "");
---

{normalizedHtml && <section class="content-block" set:html={normalizedHtml} />}
